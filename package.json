{
  "name": "ocpp-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OCPP 1.6J charge point simulator for testing the Recharge backend",
  "main": "src/index.js",
  "scripts": {
    "start": "concurrently -n backend,client \"node src/index.js\" \"cd client && npm run dev\"",
    "start:prod": "node src/index.js",
    "dev": "concurrently -n backend,client \"node --watch src/index.js\" \"cd client && npm run dev\"",
    "dev:backend": "node --watch src/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:start": "npm run build && node src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "socket.io": "^4.8.3",
    "uuid": "^10.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
